"""Stub temporaire — sera complété en Phase 5."""
from ..utils.errors import AppError


def list_materials(_args):
    raise AppError("Non implémenté.", 501)


def get_material(_mat_id):
    raise AppError("Non implémenté.", 501)


def create_material(_payload, _profile):
    raise AppError("Non implémenté.", 501)


def update_material(_mat_id, _payload, _profile):
    raise AppError("Non implémenté.", 501)


def delete_material(_mat_id, _profile):
    raise AppError("Non implémenté.", 501)